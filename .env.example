XAI_API_KEY — required; without it _llm_call returns None and each class falls through to its deterministic fallback (empty summary / unranked candidates / template explain)
XAI_MODEL — optional, defaults to grok-2-latest
XAI_API_URL — optional, defaults to https://api.x.ai/v1/chat/completions